# This file is automatically generated during configuration of the SIMDIS SDK.

# Temporarily reassign the CMAKE_MODULE_PATH so we can use find_package()
set(_OLD_CMAKE_MODULE_PATH "${CMAKE_MODULE_PATH}")
list(APPEND CMAKE_MODULE_PATH "@SDK_LIB_CMAKE_FULLPATH@")

# Set up the CMAKE_PREFIX_PATH based on directories active during the
# build of SIMDIS SDK. This restricts the use of this file to own machine
# only. Best practice is to let users specify this manually.
set(_OLD_CMAKE_PREFIX_PATH "${CMAKE_PREFIX_PATH}")
list(APPEND CMAKE_PREFIX_PATH "@CMAKE_PREFIX_PATH@")

# Locate the SIMDIS SDK configuration files from the install location
find_package(SIMDIS_SDK)

# Restore the original CMAKE_MODULE_PATH and CMAKE_PREFIX_PATH
set(CMAKE_MODULE_PATH "${_OLD_CMAKE_MODULE_PATH}")
set(CMAKE_PREFIX_PATH "${_OLD_CMAKE_PREFIX_PATH}")
unset(_OLD_CMAKE_MODULE_PATH)
unset(_OLD_CMAKE_PREFIX_PATH)
